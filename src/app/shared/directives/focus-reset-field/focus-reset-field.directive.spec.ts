import { FocusResetFieldDirective } from './focus-reset-field.directive';

describe('FocusResetFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusResetFieldDirective();
    expect(directive).toBeTruthy();
  });
});
